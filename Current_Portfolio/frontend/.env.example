# Server-side only. Add this in Vercel Project Settings, never in React code.
GROQ_API_KEY=

# Optional local override when running the separate FastAPI backend.
REACT_APP_BACKEND_URL=http://localhost:8000
